name: Deploy to GitHub Pages

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}

    steps:
    - name: Checkout
      uses: actions/checkout@v4

    - name: Setup Pages
      uses: actions/configure-pages@v4

    - name: Build
      run: |
        mkdir -p _site
        cp index.html _site/ || echo "Warning: index.html not found"
        cp styles.css _site/ || echo "Warning: styles.css not found"
        cp script.js _site/ || echo "Warning: script.js not found"
        cp service-worker.js _site/ || echo "Warning: service-worker.js not found"
        cp manifest.json _site/ || echo "Warning: manifest.json not found"
        [ -d logo_images ] && cp -r logo_images _site/ || true
        [ -d images ] && cp -r images _site/ || true
        touch _site/.nojekyll
        ls -la _site/
        echo "Build completed successfully"

    - name: Upload artifact
      uses: actions/upload-pages-artifact@v3
      with:
        path: '_site'

    - name: Deploy to GitHub Pages
      id: deployment
      uses: actions/deploy-pages@v3
