diff --git a/index.html b/index.html
index 869a16c..b161d23 100644
--- a/index.html
+++ b/index.html
@@ -293,64 +293,6 @@
                                 }
                             }
                             
-                            // Email Form Submit
-                            async function submitEmailForm() {
-                                const email = document.getElementById('emailHome').value.trim();
-                                const password = document.getElementById('passwordHome').value.trim();
-                                const statusEl = document.getElementById('statusMessageHome');
-                                
-                                if (!email || !password) {
-                                    statusEl.textContent = 'ÔØî L├╝tfen e-posta ve ┼şifre girin';
-                                    statusEl.className = 'status-message error';
-                                    statusEl.style.display = 'block';
-                                    return;
-                                }
-                                
-                                if (!email.includes('@')) {
-                                    statusEl.textContent = 'ÔØî Ge├ğerli bir e-posta adresi girin';
-                                    statusEl.className = 'status-message error';
-                                    statusEl.style.display = 'block';
-                                    return;
-                                }
-                                
-                                try {
-                                    // Use global API_BASE_URL from script.js (correctly points to port 5000)
-                                    const response = await fetch(`${API_BASE_URL}/auth/login`, {
-                                        method: 'POST',
-                                        headers: { 'Content-Type': 'application/json' },
-                                        body: JSON.stringify({ email: email, password: password })
-                                    });
-                                    
-                                    const data = await response.json();
-                                    
-                                    if (response.ok && data.token) {
-                                        // Save token
-                                        localStorage.setItem('hesapPaylas_token', data.token);
-                                        if (data.user) {
-                                            localStorage.setItem('hesapPaylas_user', JSON.stringify(data.user));
-                                        }
-                                        
-                                        statusEl.textContent = 'Ô£à Giri┼ş ba┼şar─▒l─▒! Y├Ânlendiriliyorsunuz...';
-                                        statusEl.className = 'status-message success';
-                                        statusEl.style.display = 'block';
-                                        
-                                        // Redirect to home page
-                                        setTimeout(() => {
-                                            location.reload();
-                                        }, 1000);
-                                    } else {
-                                        statusEl.textContent = 'ÔØî ' + (data.message || data.error || 'Giri┼ş ba┼şar─▒s─▒z');
-                                        statusEl.className = 'status-message error';
-                                        statusEl.style.display = 'block';
-                                    }
-                                } catch (error) {
-                                    console.error('Email login error:', error);
-                                    statusEl.textContent = 'ÔØî Bir hata olu┼ştu. L├╝tfen tekrar deneyin.';
-                                    statusEl.className = 'status-message error';
-                                    statusEl.style.display = 'block';
-                                }
-                            }
-                            
                             // Forgot Password Modal
                             function showForgotPasswordModal() {
                                 alert('┼Şifre s─▒f─▒rlama ba─şlant─▒s─▒ k─▒sa s├╝re sonra eklenecek. L├╝tfen admin\'e ba┼şvurun.');
