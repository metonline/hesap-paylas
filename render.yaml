services:
  - type: web
    name: hesap-paylas-api
    env: python
    buildCommand: pip install -r requirements.txt && echo "Build timestamp $(date)" 
    startCommand: sh start.sh
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: hesap-paylas-db
          property: connectionString

  - type: postgres
    name: hesap-paylas-db
    ipWhitelist: []
    plan: free
    postgresMajorVersion: 15

