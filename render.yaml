services:
  - type: web
    name: hesap-paylas-api
    env: python
    # Force rebuild with timestamp - should trigger fresh deploy
    buildCommand: echo "Build ID $(date +%s%N)" && pip install -r requirements.txt
    startCommand: sh start.sh
    envVars:
      - key: RENDER_DATABASE_URL
        fromDatabase:
          name: hesap-paylas-db
          property: connectionString
      - key: DATABASE_URL
        fromDatabase:
          name: hesap-paylas-db
          property: connectionString

  - type: postgres
    name: hesap-paylas-db
    ipWhitelist: []
    plan: free
    postgresMajorVersion: 15

